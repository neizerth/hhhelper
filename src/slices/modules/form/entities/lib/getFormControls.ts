import type { IFormControl } from "../../shared/model";

// const validators

export const getFormControls = (
  _form: HTMLFormElement,
): IFormControl<HTMLInputElement, boolean | string>[] => {
  // const { elements } = form;

  return [];
};
